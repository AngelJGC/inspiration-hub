# framer/motion — official examples

> **Authoritative reference when a pattern must be "correct" across React versions.**

- **URL:** https://github.com/framer/motion/tree/main/packages/framer-motion/dev/examples
- **Stars (parent):** ⭐ 24k+
- **Stack:** React · Framer Motion

## Canonical patterns

- **`layout` + `layoutId` shared-element transitions** — the magic move pattern (FLIP).
- **`AnimatePresence` `mode="popLayout"`** for list reordering without jump cuts.
- **`useScroll` / `useSpring` stiffness presets** — when to use which.
- **Gesture composition** — `drag` + `whileHover` + `whileTap` on the same element.

## Why inspiring

Maintained by the Framer team. If you're debating "does `layoutId` re-measure on parent scroll?"
or "what's the right `spring` config for an iOS-feeling pop?" — **read the source, not a blog
post**. Every example here is versioned against the library itself.

## Cross-refs

- Pick a specific technique: `10-framer-showcase/olivierlarose-framer-motion-tutorials`.
- Component library: `magicui`, `aceternity-ui`.
